{
  "name": "content-os-functions",
  "private": true,
  "description": "Content OS serverless backend — holds the LLM API keys and calls Claude/OpenAI/Gemini.",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@google/generative-ai": "^0.21.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
